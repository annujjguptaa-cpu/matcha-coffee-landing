/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // Required for GitHub Pages
    images: {
        unoptimized: true, // Required to show your coffee frames on GitHub
    },
};

export default nextConfig;